<script setup>
import { withWorker } from "./withWorker.js";
import { withoutWorker } from "./withoutWorker.js";

const { do: doWithWorker } = withWorker();
const { do: doWithoutWorker } = withoutWorker();
</script>

<template>
  <header>
    <img
      alt="Vue logo"
      class="logo"
      src="./assets/logo.svg"
      width="125"
      height="125"
    />
  </header>

  <main>
    <button @click="doWithWorker">Worker</button>
    <button @click="doWithoutWorker">NO Worker</button>
  </main>
</template>

<style scoped>
header {
  line-height: 1.5;
}

.logo {
  display: block;
  margin: 0 auto 2rem;
}

@media (min-width: 1024px) {
  header {
    display: flex;
    place-items: center;
    padding-right: calc(var(--section-gap) / 2);
  }

  .logo {
    margin: 0 2rem 0 0;
  }

  header .wrapper {
    display: flex;
    place-items: flex-start;
    flex-wrap: wrap;
  }
}
</style>
